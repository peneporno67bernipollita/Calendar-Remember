<?xml version="1.0" encoding="utf-8"?>
<!-- Fondo propio de la agenda en la notificacion: el negro de la app, para
     que el neon se lea igual con el tema claro que con el oscuro de MIUI. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#0A0E18" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="#1B2336" />
</shape>
